uniform sampler2D inputImageTexture; uniform sampler2D skinMaskTexture; varying highp vec2 textureCoordinate; varying vec2 textureCoordinateFace; varying highp vec4 textureShift_1; varying highp vec4 textureShift_2; varying highp vec4 textureShift_3; uniform highp float THRESHOLD; uniform highp vec2 texelOffsetFace; uniform float weight_thredshold; const mat3 matrgb2lab = mat3( 0.299, 0.587, 0.114, -0.169, -0.331, 0.500, 0.500, -0.419, -0.081); const mat3 matlab2rgb = mat3(1.0, -0.000927, 1.402, 1.0, -0.344, -0.714, 1.0, 1.772, 0.000990); void main() { highp vec3 centerColor = texture2D(inputImageTexture, textureCoordinate).rgb; highp vec3 centerlabColor = centerColor*matrgb2lab; highp vec3 sum = centerlabColor; highp vec3 norm = vec3(1.0); mediump vec3 diff = vec3(0.0); highp vec3 curColor = vec3(0.0); curColor = texture2D(inputImageTexture, textureShift_1.xy).rgb; highp vec3 labColor = curColor*matrgb2lab; lowp float skinMask = texture2D(skinMaskTexture, textureCoordinateFace - texelOffsetFace).r; float dis = distance(centerlabColor.gb, labColor.gb); diff = clamp(vec3(1.0) - vec3(abs(centerlabColor.r-labColor.r), dis, dis) * THRESHOLD, vec3(0.0), vec3(1.0)) * max(weight_thredshold, skinMask); sum += diff * labColor; norm += diff; curColor = texture2D(inputImageTexture, textureShift_1.zw).rgb; labColor = curColor*matrgb2lab; skinMask = texture2D(skinMaskTexture, textureCoordinateFace + texelOffsetFace).r; dis = distance(centerlabColor.gb, labColor.gb); diff = clamp(vec3(1.0) - vec3(abs(centerlabColor.r-labColor.r), dis, dis) * THRESHOLD, vec3(0.0), vec3(1.0)) * max(weight_thredshold, skinMask); sum += diff * labColor; norm += diff; curColor = texture2D(inputImageTexture, textureShift_2.xy).rgb; labColor = curColor*matrgb2lab; skinMask = texture2D(skinMaskTexture, textureCoordinateFace - 2.0 * texelOffsetFace).r; dis = distance(centerlabColor.gb, labColor.gb); diff = clamp(vec3(1.0) - vec3(abs(centerlabColor.r-labColor.r), dis, dis) * THRESHOLD, vec3(0.0), vec3(1.0)) * max(weight_thredshold, skinMask); sum += diff * labColor; norm += diff; curColor = texture2D(inputImageTexture, textureShift_2.zw).rgb; labColor = curColor*matrgb2lab; skinMask = texture2D(skinMaskTexture, textureCoordinateFace + 2.0 * texelOffsetFace).r; dis = distance(centerlabColor.gb, labColor.gb); diff = clamp(vec3(1.0) - vec3(abs(centerlabColor.r-labColor.r), dis, dis) * THRESHOLD, vec3(0.0), vec3(1.0)) * max(weight_thredshold, skinMask); sum += diff * labColor; norm += diff; curColor = texture2D(inputImageTexture, textureShift_3.xy).rgb; labColor = curColor*matrgb2lab; skinMask = texture2D(skinMaskTexture, textureCoordinateFace - 3.0 * texelOffsetFace).r; dis = distance(centerlabColor.gb, labColor.gb); diff = clamp(vec3(1.0) - vec3(abs(centerlabColor.r-labColor.r), dis, dis) * THRESHOLD, vec3(0.0), vec3(1.0)) * max(weight_thredshold, skinMask); sum += diff * labColor; norm += diff; curColor = texture2D(inputImageTexture, textureShift_3.zw).rgb; labColor = curColor*matrgb2lab; skinMask = texture2D(skinMaskTexture, textureCoordinateFace + 3.0 * texelOffsetFace).r; dis = distance(centerlabColor.gb, labColor.gb); diff = clamp(vec3(1.0) - vec3(abs(centerlabColor.r-labColor.r), dis, dis) * THRESHOLD, vec3(0.0), vec3(1.0)) * max(weight_thredshold, skinMask); sum += diff * labColor; norm += diff; vec3 resultColor = sum / norm; resultColor = resultColor*matlab2rgb; gl_FragColor = vec4(resultColor, 1.0); }