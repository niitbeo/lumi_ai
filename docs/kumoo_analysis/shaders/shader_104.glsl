varying highp vec2 textureCoordinate; uniform sampler2D inputImageTexture; uniform sampler2D baseColorTexture; uniform sampler2D whiteColorTexture; uniform sampler2D skinMaskTexture; uniform sampler2D faceParsingMaskTexture; uniform sampler2D instanceMaskTexture; uniform lowp float r_threshold1; uniform lowp float r_threshold2; uniform lowp float g_threshold1; uniform lowp float g_threshold2; uniform lowp float colorBaseFaceAlpha; uniform lowp float colorBaseBodyAlpha; uniform lowp float colorWhitenFaceAlpha; uniform lowp float colorWhitenBodyAlpha; void main() { highp vec4 textureColor = texture2D(inputImageTexture, textureCoordinate); lowp float faceParsingMask = texture2D(faceParsingMaskTexture, textureCoordinate).a; lowp float skinMask = texture2D(skinMaskTexture, textureCoordinate).r; lowp vec2 instanceMask = texture2D(instanceMaskTexture, textureCoordinate).rg; lowp float alpha1 = step(r_threshold1, instanceMask.r) * step(instanceMask.r, r_threshold2); lowp float alpha2 = step(g_threshold1, instanceMask.g) * step(instanceMask.g, g_threshold2); lowp float bodyMask = (1.0 - faceParsingMask) * skinMask * alpha1 * alpha2; lowp float faceMask = faceParsingMask * skinMask * alpha1 * alpha2; highp float blueColor = textureColor.b * 63.0; highp vec2 quad1; quad1.y = floor(floor(blueColor) / 8.0); quad1.x = floor(blueColor) - (quad1.y * 8.0); highp vec2 quad2; quad2.y = floor(ceil(blueColor) / 8.0); quad2.x = ceil(blueColor) - (quad2.y * 8.0); highp vec2 texPos1; texPos1.x = (quad1.x * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.r); texPos1.y = (quad1.y * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.g); highp vec2 texPos2; texPos2.x = (quad2.x * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.r); texPos2.y = (quad2.y * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.g); lowp vec3 newColorBase1 = texture2D(baseColorTexture, texPos1).rgb; lowp vec3 newColorBase2 = texture2D(baseColorTexture, texPos2).rgb; lowp vec3 newColorBase = mix(newColorBase1, newColorBase2, fract(blueColor)); lowp vec3 afterBaseFaceColor = mix(textureColor.rgb, newColorBase, faceMask * colorBaseFaceAlpha); lowp vec3 afterBaseBodyColor = mix(afterBaseFaceColor, newColorBase, bodyMask * colorBaseBodyAlpha); highp float blueColorWhiten = afterBaseBodyColor.b * 63.0; highp vec2 quadWhiten1; quadWhiten1.y = floor(floor(blueColorWhiten) / 8.0); quadWhiten1.x = floor(blueColorWhiten) - (quadWhiten1.y * 8.0); highp vec2 quadWhiten2; quadWhiten2.y = floor(ceil(blueColorWhiten) / 8.0); quadWhiten2.x = ceil(blueColorWhiten) - (quadWhiten2.y * 8.0); highp vec2 texPosWhiten1; texPosWhiten1.x = (quadWhiten1.x * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * afterBaseBodyColor.r); texPosWhiten1.y = (quadWhiten1.y * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * afterBaseBodyColor.g); highp vec2 texPosWhiten2; texPosWhiten2.x = (quadWhiten2.x * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * afterBaseBodyColor.r); texPosWhiten2.y = (quadWhiten2.y * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * afterBaseBodyColor.g); lowp vec3 newColorWhiten1 = texture2D(whiteColorTexture, texPosWhiten1).rgb; lowp vec3 newColorWhiten2 = texture2D(whiteColorTexture, texPosWhiten2).rgb; lowp vec3 newColorWhiten = mix(newColorWhiten1, newColorWhiten2, fract(blueColorWhiten)); lowp vec3 afterWhitenFaceColor = mix(afterBaseBodyColor, newColorWhiten, faceMask * colorWhitenFaceAlpha); gl_FragColor = vec4(mix(afterWhitenFaceColor, newColorWhiten, bodyMask * colorWhitenBodyAlpha), textureColor.a); }