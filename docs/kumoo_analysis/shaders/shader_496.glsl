varying highp vec2 textureCoordinate; uniform sampler2D inputImageTexture; uniform sampler2D effectMaskTexture; uniform sampler2D lutTexture; uniform lowp float alpha; uniform lowp float useOriLut; const mat3 matrgb2YCbCr = mat3(0.299, 0.587, 0.114, -0.169, -0.331, 0.500, 0.500, -0.419, -0.081); const mat3 matYCbCr2rgb = mat3(1.0, -0.000927, 1.402, 1.0, -0.344, -0.714, 1.0, 1.772, 0.000990); void main() { lowp vec4 textureColor = texture2D(inputImageTexture, textureCoordinate); lowp float effectMaskColor = texture2D(effectMaskTexture, textureCoordinate).g; highp float blueColor = textureColor.b * 63.0; highp vec2 quad1; quad1.y = floor(floor(blueColor) / 8.0); quad1.x = floor(blueColor) - (quad1.y * 8.0); highp vec2 quad2; quad2.y = floor(ceil(blueColor) / 8.0); quad2.x = ceil(blueColor) - (quad2.y * 8.0); highp vec2 texPos1; texPos1.x = (quad1.x * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.r); texPos1.y = (quad1.y * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.g); highp vec2 texPos2; texPos2.x = (quad2.x * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.r); texPos2.y = (quad2.y * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.g); lowp vec3 newColor1 = texture2D(lutTexture, texPos1).rgb; lowp vec3 newColor2 = texture2D(lutTexture, texPos2).rgb; lowp vec3 newColor = mix(newColor1, newColor2, fract(blueColor)); lowp vec3 newColorOut; if (useOriLut == 0.0) { lowp vec2 textureColorYCbCr = vec3(textureColor.rgb * matrgb2YCbCr).gb; lowp float newColorYCbCrLuma = vec3(newColor * matrgb2YCbCr).r; newColorOut = vec3(newColorYCbCrLuma, textureColorYCbCr) * matYCbCr2rgb; } else { newColorOut = newColor; } gl_FragColor = vec4(mix(textureColor.rgb, newColorOut, alpha * effectMaskColor), textureColor.a); }