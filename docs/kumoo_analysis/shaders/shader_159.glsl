varying highp vec2 textureCoordinate; uniform sampler2D inputImageTexture; uniform sampler2D facialBeautifyMaskTexture; uniform sampler2D eyeMaskTexture; uniform lowp float brightEyeAlpha; uniform lowp float brightEyeShadowAlpha; uniform highp float stepSize1; uniform highp float stepSize2; void main() { lowp vec3 sum; lowp vec3 color; lowp vec4 iColor = texture2D(inputImageTexture, textureCoordinate); lowp vec3 eyeColor = texture2D(eyeMaskTexture, textureCoordinate).rgb; lowp float eyeMask = max(max(eyeColor.r, eyeColor.g), eyeColor.b); lowp vec3 faceMaskColor = texture2D(facialBeautifyMaskTexture, textureCoordinate).rgb; color = iColor.rgb; mediump float p = 0.0; mediump float t = 0.0; highp vec3 sumColor = vec3(0.0, 0.0, 0.0); for(t = -2.0; t < 2.5; t += 1.0){ for(p = -2.0;p < 2.5;p += 1.0) { sumColor += texture2D(inputImageTexture, textureCoordinate + t * vec2(stepSize1, 0.0) + p * vec2(0.0, stepSize2)).rgb; } } sumColor = sumColor * 0.04; sumColor = clamp(sumColor + (color.rgb - sumColor) * 3.0, 0.0, 1.0); sumColor.r = max(color.r, sumColor.r); sumColor.g = max(color.g, sumColor.g); sumColor.b = max(color.b, sumColor.b); color = mix(color, sumColor, brightEyeAlpha * faceMaskColor.g * eyeMask); lowp vec3 grayColor = color; lowp vec3 brightColor = clamp(grayColor * grayColor * -0.3543 + 1.354 * grayColor, 0.0, 1.0); color = mix(grayColor, brightColor, faceMaskColor.b * brightEyeShadowAlpha * eyeMask); lowp vec3 shadowColor = clamp(grayColor * grayColor * 0.3401 + 0.6599 * grayColor, 0.0, 1.0); color = mix(color, shadowColor, faceMaskColor.r * brightEyeShadowAlpha * eyeMask); gl_FragColor = vec4(color, iColor.a); }