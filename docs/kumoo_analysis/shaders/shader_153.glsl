varying vec2 textureCoordinateStdFace; varying vec3 textureCoordinate; uniform float jawlineShadowAlpha; uniform sampler2D inputImageTexture; uniform sampler2D jawlineShadowMaskTexture; uniform sampler2D skinMaskTexture; uniform sampler2D faceParsingMaskTexture; uniform sampler2D instanceMaskTexture; uniform float r_threshold1; uniform float r_threshold2; uniform float g_threshold1; uniform float g_threshold2; lowp float blendSoftLight(float base, float blend) { lowp float color1 = 2.0 * base * blend + base * base * (1.0 - 2.0 * blend); lowp float color2 = sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend); lowp float mixColor = mix(color1, color2, step(0.5, blend)); return mixColor; } void main() { lowp vec4 origColor = texture2D(inputImageTexture, textureCoordinate.xy); lowp vec3 shadowColor = texture2D(jawlineShadowMaskTexture, textureCoordinateStdFace).rgb; lowp float skinColor = texture2D(skinMaskTexture, textureCoordinate.xy).g; lowp float faceParsingColor = texture2D(faceParsingMaskTexture, textureCoordinate.xy).a; lowp vec2 instanceMaskColor = texture2D(instanceMaskTexture, textureCoordinate.xy).rg; lowp float alpha1 = step(r_threshold1, instanceMaskColor.r) * step(instanceMaskColor.r, r_threshold2); lowp float alpha2 = step(g_threshold1, instanceMaskColor.g) * step(instanceMaskColor.g, g_threshold2); lowp float instanceAlpha = alpha1 * alpha2; lowp vec3 newColor = vec3(0.0); newColor.r = blendSoftLight(origColor.r, shadowColor.r); newColor.g = blendSoftLight(origColor.g, shadowColor.g); newColor.b = blendSoftLight(origColor.b, shadowColor.b); lowp float alpha = smoothstep(0.0, 0.9, textureCoordinate.z) * jawlineShadowAlpha * skinColor * (1.0-faceParsingColor) * instanceAlpha; gl_FragColor = vec4(mix(origColor.rgb, newColor.rgb, alpha), origColor.a); }