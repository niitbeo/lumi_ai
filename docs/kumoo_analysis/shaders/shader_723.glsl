uniform sampler2D inputImageTexture; uniform sampler2D lowPassTexture; uniform sampler2D resTexture; uniform sampler2D highPassTexture; uniform sampler2D highResTexture; uniform sampler2D lowResTexture; uniform sampler2D wholeFaceMaskTexture; uniform sampler2D skinMaskTexture; uniform float blurHightAlpha; uniform float blurLowAlpha; varying highp vec2 textureCoordinateCrop; varying vec2 textureCoordinateOri; varying vec2 textureCoordinateParsing; void main() { mediump vec4 oriColor = texture2D(inputImageTexture, textureCoordinateOri); mediump vec3 lowPassColor = texture2D(lowPassTexture, textureCoordinateCrop).rgb; mediump vec3 resColor = texture2D(resTexture, textureCoordinateCrop).rgb; mediump vec3 highPassColor = texture2D(highPassTexture, textureCoordinateCrop).rgb; mediump vec3 lowResColor = texture2D(lowResTexture, textureCoordinateCrop).rgb; mediump vec3 highResColor = texture2D(highResTexture, textureCoordinateCrop).rgb; lowp float edge_mask = step(0.01, textureCoordinateCrop.x) * step(0.01, textureCoordinateCrop.y) * step(textureCoordinateCrop.x, 0.99) * step(textureCoordinateCrop.y, 0.99); lowp float skinMask = texture2D(skinMaskTexture, textureCoordinateCrop).r; lowp float wholeface = texture2D(wholeFaceMaskTexture, textureCoordinateParsing).r; mediump float other_mask = wholeface * edge_mask * skinMask; float blurHightAlpha_abs = abs(blurHightAlpha); mediump vec3 highFreqPart; float isBlurHightPositive = step(0.0, blurHightAlpha); mediump vec3 highFreqNegative = oriColor.rgb - blurHightAlpha_abs * (highPassColor - lowPassColor); mediump vec3 highFreqPositive = oriColor.rgb + 3.0 * blurHightAlpha * (highPassColor - highResColor); highFreqPart = mix(highFreqNegative, highFreqPositive, isBlurHightPositive); mediump vec3 color = highFreqPart - blurLowAlpha * (lowPassColor - resColor); mediump vec3 lowMinusHighRes2 = lowPassColor - lowResColor; if(blurHightAlpha < 0.0){ color = color - lowMinusHighRes2 * max(blurHightAlpha_abs, blurLowAlpha); } else { color = color - lowMinusHighRes2 * blurLowAlpha; } gl_FragColor = vec4(mix(oriColor.rgb, color, other_mask), oriColor.a); }