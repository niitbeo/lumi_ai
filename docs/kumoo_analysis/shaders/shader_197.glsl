varying highp vec2 textureCoordinate; varying highp vec2 textureCoordinate2; uniform sampler2D inputImageTexture; uniform sampler2D inputImageTexture2; uniform sampler2D faceSkinMaskTexture; uniform highp float widthOffset; uniform highp float heightOffset; uniform highp float sharpenAlpha; uniform lowp float sharpenFaceAlpha; uniform highp float sharpenOrganAlpha; void main() { highp vec4 iColor = texture2D(inputImageTexture2, textureCoordinate2); lowp vec3 faceSkinMask = texture2D(faceSkinMaskTexture, textureCoordinate).rgb; lowp float skinMask = 1.0 - min(faceSkinMask.g,faceSkinMask.r); highp float sum = 0.25 * iColor.g; sum += 0.125 * texture2D(inputImageTexture2, textureCoordinate + vec2(-widthOffset, 0.0)) .g; sum += 0.125 * texture2D(inputImageTexture2, textureCoordinate + vec2(widthOffset, 0.0)) .g; sum += 0.125 * texture2D(inputImageTexture2, textureCoordinate + vec2(0.0, -heightOffset)) .g; sum += 0.125 * texture2D(inputImageTexture2, textureCoordinate + vec2(0.0, heightOffset)) .g; sum += 0.0625 * texture2D(inputImageTexture2, textureCoordinate + vec2(widthOffset, heightOffset)) .g; sum += 0.0625 * texture2D(inputImageTexture2, textureCoordinate + vec2(-widthOffset, -heightOffset)) .g; sum += 0.0625 * texture2D(inputImageTexture2, textureCoordinate + vec2(-widthOffset, heightOffset)) .g; sum += 0.0625 * texture2D(inputImageTexture2, textureCoordinate + vec2(widthOffset, -heightOffset)) .g; highp vec4 resultColor = texture2D(inputImageTexture, textureCoordinate); highp vec3 color = clamp(resultColor.rgb + 2.0 * (iColor.g - sum), 0.0, 1.0); lowp float sharpenRegion = mix(1.0, skinMask, sharpenFaceAlpha); highp float sharpen_face_alpha = mix(sharpenAlpha, sharpenAlpha*sharpenOrganAlpha, (1.0-skinMask)*faceSkinMask.b); mediump float alpha = 1.0; 
#if defined NEW_SHARPEN 
 alpha = 1.0 - clamp(((iColor.g - sum) - 0.01) / 0.02, 0.0, 1.0); 
#endif 
 color = mix(resultColor.rgb, color.rgb, sharpen_face_alpha * sharpenRegion * alpha); gl_FragColor = vec4(color, iColor.a); }