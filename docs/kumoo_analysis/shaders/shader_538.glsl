uniform sampler2D inputImageTexture; uniform sampler2D inputImageTexture2; varying highp vec2 textureCoordinate; uniform sampler2D skinMaskTexture; uniform sampler2D wakeSkinMaskTexture; uniform lowp float sharpenAlpha; uniform lowp float eyebrowSharpenAlpha; uniform lowp float isMouth; void main() { lowp vec4 iColor = texture2D(inputImageTexture, textureCoordinate).rgba; lowp float eyesMask = texture2D(inputImageTexture2, textureCoordinate).r; lowp float skinMask = texture2D(skinMaskTexture, textureCoordinate).a; lowp vec3 wakeMask = texture2D(wakeSkinMaskTexture, textureCoordinate).rgb; lowp vec3 color = iColor.rgb; mediump vec3 newColor = color; float ratio = color.r - min(color.b, color.g); highp float red_alpha = 1.0; float min_color = min(newColor.g, min(newColor.r, newColor.b)); mediump float delta = min_color * 0.6; highp float e = 1.0e-10; if(newColor.g > newColor.b && newColor.g > newColor.r) { float hue = (newColor.b - newColor.r) / (newColor.g - min_color + e); if(newColor.r > newColor.b) { newColor.b -= delta; newColor.r = newColor.b - (newColor.g - newColor.b) * hue; } else { newColor.r -= delta; newColor.b = newColor.r + (newColor.g - newColor.r) * hue; } red_alpha = clamp((newColor.g - min_color) * 3.0, 0.0, 1.0); } else if(newColor.r > newColor.b && newColor.r > newColor.g) { float hue = (newColor.g - newColor.b) / (newColor.r - min_color + e); if(newColor.g > newColor.b) { newColor.b -= delta; newColor.g = newColor.b + (newColor.r - newColor.b) * hue; } else { newColor.g -= delta; newColor.b = newColor.g - (newColor.r - newColor.g) * hue; } red_alpha = clamp((newColor.r - min_color) * 3.0, 0.0, 1.0); } else if(newColor.b > newColor.r && newColor.b > newColor.g) { float hue = (newColor.r - newColor.g) / (newColor.b - min_color + e); if(newColor.g > newColor.r) { newColor.r -= delta; newColor.g = newColor.r - (newColor.b - newColor.r) * hue; } else { newColor.g -= delta; newColor.r = newColor.g + (newColor.b - newColor.g) * hue; } red_alpha = clamp((newColor.b - min_color) * 3.0, 0.0, 1.0); } mediump vec3 mouthColor = mix(color, newColor, 0.75); mouthColor = mouthColor * 0.925; color = mix(color, mouthColor, wakeMask.b * sharpenAlpha * eyesMask * red_alpha * (1.0-isMouth) * skinMask); color = mix(color, mouthColor, wakeMask.r * sharpenAlpha * eyesMask * red_alpha * isMouth * skinMask); color = mix(color, color - vec3(delta * 0.25), wakeMask.r * eyebrowSharpenAlpha * skinMask); gl_FragColor = vec4(color, iColor.a); }