varying highp vec2 textureCoordinate; uniform sampler2D inputImageTexture; uniform sampler2D inputImageTexture2; uniform sampler2D facialBeautifyMaskTexture; uniform sampler2D skinMaskTexture; uniform highp float widthOffset; uniform highp float heightOffset; uniform highp float sharpenEyeAlpha; uniform highp float contrastEyeAlpha; uniform highp float regionType; lowp float blendSoftLight(float base, float blend) { lowp float color1 = 2.0 * base * blend + base * base * (1.0 - 2.0 * blend); lowp float color2 = sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend); return mix(color1, color2, step(0.5, blend)); } void main() { highp vec4 iColor = texture2D(inputImageTexture, textureCoordinate); highp vec3 gaussColor = texture2D(inputImageTexture2, textureCoordinate).rgb; lowp float brightEyeMask = texture2D(facialBeautifyMaskTexture, textureCoordinate).g; lowp float skinMask = texture2D(skinMaskTexture, textureCoordinate).a; highp float sum = 0.25 * iColor.g; sum += 0.125 * texture2D(inputImageTexture, textureCoordinate + vec2(-widthOffset, 0.0)) .g; sum += 0.125 * texture2D(inputImageTexture, textureCoordinate + vec2(widthOffset, 0.0)) .g; sum += 0.125 * texture2D(inputImageTexture, textureCoordinate + vec2(0.0, -heightOffset)) .g; sum += 0.125 * texture2D(inputImageTexture, textureCoordinate + vec2(0.0, heightOffset)) .g; sum += 0.0625 * texture2D(inputImageTexture, textureCoordinate + vec2(widthOffset, heightOffset)) .g; sum += 0.0625 * texture2D(inputImageTexture, textureCoordinate + vec2(-widthOffset, -heightOffset)) .g; sum += 0.0625 * texture2D(inputImageTexture, textureCoordinate + vec2(-widthOffset, heightOffset)) .g; sum += 0.0625 * texture2D(inputImageTexture, textureCoordinate + vec2(widthOffset, -heightOffset)) .g; highp vec3 color = clamp(iColor.rgb + 2.0 * (iColor.g - sum), 0.0, 1.0); mediump float alpha = 1.0 - clamp(((iColor.g - sum) - 0.03) / 0.3, 0.0, 1.0); float mask = mix(brightEyeMask, 1.0, regionType) * skinMask; color = mix(iColor.rgb, color.rgb, sharpenEyeAlpha*alpha*mask); color = mix(color, (color - 0.6*gaussColor)/0.4, contrastEyeAlpha*0.6*mask); gl_FragColor = vec4(color, iColor.a); }