varying highp vec4 textureCoordinate2; uniform sampler2D faceMaskTexture; uniform highp float blurAlpha; void main() { vec4 srcColor = gl_LastFragData[0]; lowp vec4 faceMask = texture2D(faceMaskTexture, textureCoordinate2.xy); float alpha = blurAlpha*step(0.0001, max(max(faceMask.r,faceMask.g),faceMask.b)); faceMask.r = max(srcColor.r, faceMask.r); faceMask.g = max(srcColor.g, faceMask.g); faceMask.b = max(srcColor.b, faceMask.b); faceMask.a = max(srcColor.a, alpha); gl_FragColor = vec4(faceMask); }