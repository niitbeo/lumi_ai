uniform sampler2D inputImageTexture; varying highp vec2 textureCoordinate; uniform lowp float stepSize1; uniform lowp float stepSize2; void main() { highp vec3 sum = vec3(0.0); lowp float count = 0.0; for (lowp float j = -1.0; j < 2.0; j += 1.0) { for (lowp float i = -1.0; i < 2.0; i += 1.0) { highp vec3 color = texture2D(inputImageTexture, textureCoordinate + vec2(stepSize1, stepSize2) * vec2(i, j)).rgb; sum += color; count += 1.0; } } gl_FragColor = vec4(sum/count, 1.0); }