uniform sampler2D inputImageTexture; varying highp vec2 textureCoordinate; varying highp vec4 textureShift_1; varying highp vec4 textureShift_2; varying highp vec4 textureShift_3; uniform highp float THRESHOLD; uniform float glassAlpha; void main() { highp vec3 centerColor = texture2D(inputImageTexture, textureCoordinate).rgb; highp vec3 sum = centerColor; highp vec3 norm = vec3(1.0); mediump vec3 diff = vec3(0.0); highp vec3 curColor = vec3(0.0); curColor = texture2D(inputImageTexture, textureShift_1.xy).rgb; diff = clamp(vec3(1.0) - abs(centerColor - curColor) * THRESHOLD, vec3(0.0), vec3(1.0)); sum += diff * curColor; norm += diff; curColor = texture2D(inputImageTexture, textureShift_1.zw).rgb; diff = clamp(vec3(1.0) - abs(centerColor - curColor) * THRESHOLD, vec3(0.0), vec3(1.0)); sum += diff * curColor; norm += diff; curColor = texture2D(inputImageTexture, textureShift_2.xy).rgb; diff = clamp(vec3(1.0) - abs(centerColor - curColor) * THRESHOLD, vec3(0.0), vec3(1.0)); sum += diff * curColor; norm += diff; curColor = texture2D(inputImageTexture, textureShift_2.zw).rgb; diff = clamp(vec3(1.0) - abs(centerColor - curColor) * THRESHOLD, vec3(0.0), vec3(1.0)); sum += diff * curColor; norm += diff; curColor = texture2D(inputImageTexture, textureShift_3.xy).rgb; diff = clamp(vec3(1.0) - abs(centerColor - curColor) * THRESHOLD, vec3(0.0), vec3(1.0)); sum += diff * curColor; norm += diff; curColor = texture2D(inputImageTexture, textureShift_3.zw).rgb; diff = clamp(vec3(1.0) - abs(centerColor - curColor) * THRESHOLD, vec3(0.0), vec3(1.0)); sum += diff * curColor; norm += diff; vec3 resultColor = mix(centerColor, sum / norm, glassAlpha); gl_FragColor = vec4(resultColor, 1.0); }