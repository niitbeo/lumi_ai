varying highp vec2 textureCoordinate; uniform sampler2D inputImageTexture; uniform sampler2D faceColorTexture; uniform sampler2D bodyColorTexture; uniform sampler2D skinMaskTexture; uniform sampler2D faceParsingMaskTexture; uniform sampler2D instanceMaskTexture; uniform lowp float r_threshold1; uniform lowp float r_threshold2; uniform lowp float g_threshold1; uniform lowp float g_threshold2; uniform lowp float colorBaseBodyAlpha; uniform lowp float colorBaseFaceAlpha; void main() { highp vec4 textureColor = texture2D(inputImageTexture, textureCoordinate); lowp float faceParsingMask = texture2D(faceParsingMaskTexture, textureCoordinate).a; lowp float skinMask = texture2D(skinMaskTexture, textureCoordinate).r; highp float blueColor = textureColor.b * 63.0; highp vec2 quad1; quad1.y = floor(floor(blueColor) / 8.0); quad1.x = floor(blueColor) - (quad1.y * 8.0); highp vec2 quad2; quad2.y = floor(ceil(blueColor) / 8.0); quad2.x = ceil(blueColor) - (quad2.y * 8.0); highp vec2 texPos1; texPos1.x = (quad1.x * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.r); texPos1.y = (quad1.y * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.g); highp vec2 texPos2; texPos2.x = (quad2.x * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.r); texPos2.y = (quad2.y * 0.125) + 0.5 / 512.0 + ((0.125 - 1.0 / 512.0) * textureColor.g); lowp vec3 newFaceColor1 = texture2D(faceColorTexture, texPos1).rgb; lowp vec3 newFaceColor2 = texture2D(faceColorTexture, texPos2).rgb; lowp vec3 newFaceColor = mix(newFaceColor1, newFaceColor2, fract(blueColor)); lowp vec3 newBodyColor1 = texture2D(bodyColorTexture, texPos1).rgb; lowp vec3 newBodyColor2 = texture2D(bodyColorTexture, texPos2).rgb; lowp vec3 newBodyColor = mix(newBodyColor1, newBodyColor2, fract(blueColor)); lowp vec2 instanceMask = texture2D(instanceMaskTexture, textureCoordinate).rg; lowp float alpha1 = step(r_threshold1, instanceMask.r) * step(instanceMask.r, r_threshold2); lowp float alpha2 = step(g_threshold1, instanceMask.g) * step(instanceMask.g, g_threshold2); lowp float bodyMask = (1.0 - faceParsingMask) * skinMask * alpha1 * alpha2; lowp float faceMask = faceParsingMask * skinMask * alpha1 * alpha2; lowp vec3 afterFaceColor = mix(textureColor.rgb, newFaceColor, faceMask * colorBaseFaceAlpha); gl_FragColor = vec4(mix(afterFaceColor, newBodyColor, bodyMask * colorBaseBodyAlpha), textureColor.a); }