float blendSoftLight(float base, float blend) { float color1 = 2.0 * base * blend + base * base * (1.0 - 2.0 * blend); float color2 = sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend); return mix(color1, color2, step(0.5, blend)); } varying highp vec2 textureCoordinate; varying highp vec2 textureCoordinateStd; uniform sampler2D inputImageTexture; uniform sampler2D stdFaceMaskTexture; uniform sampler2D skinMaskTexture; uniform lowp float shadowLightAlpha; uniform sampler2D instanceMaskTexture; uniform float r_threshold1; uniform float r_threshold2; uniform float g_threshold1; uniform float g_threshold2; void main() { lowp vec4 iColor = texture2D(inputImageTexture, textureCoordinate); lowp float faceMask = texture2D(stdFaceMaskTexture, textureCoordinateStd).r; lowp float skinMask = texture2D(skinMaskTexture, textureCoordinate).r; float r = blendSoftLight(iColor.r, faceMask); float g = blendSoftLight(iColor.g, faceMask); float b = blendSoftLight(iColor.b, faceMask); lowp vec2 instanceMaskColor = texture2D(instanceMaskTexture, textureCoordinate).rg; lowp float alpha1 = step(r_threshold1, instanceMaskColor.r) * step(instanceMaskColor.r, r_threshold2); lowp float alpha2 = step(g_threshold1, instanceMaskColor.g) * step(instanceMaskColor.g, g_threshold2); lowp float instanceAlpha = alpha1 * alpha2; highp vec3 color = mix(iColor.rgb, vec3(r, g, b), clamp(shadowLightAlpha * 1.2 * skinMask * instanceAlpha, 0.0, 1.0)); gl_FragColor = vec4(color, iColor.a); }